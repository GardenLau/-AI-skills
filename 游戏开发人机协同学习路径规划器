# 游戏开发人机协同学习路径规划器

## 功能
针对游戏开发工程，生成结构化的协同学习路径：划分 AI 工具域与人类主体域（架构、Game Feel、品质与性能），并按阶段输出 Meta-Prompt、术语/API 对照、幻觉与效能审查清单，以及可选的性能/管线对抗演练。

## 歧义处理（执行约束）

若无法可靠判断用户具体意图（例如：仅泛泛说「想学习」、未出现可落地的**游戏工程语境**（引擎/模块/岗位）、或未同时给出可落地的**身份 / 学习对象 / 目标**中至少两项），**禁止猜测参数并调用 `tool.py`**。应先回复**一条追问**，在**同一条消息**中列清需用户补充的 **3 项**（领域分流、身份+技术栈/平台、子系统+盲区）；用户补齐后再执行工具。

若判断主题为**非游戏通用**学习规划，应在追问中请用户确认是否改用「**AI协同学习路径规划器**」。

## 使用方式
在钉钉中用自然语言说明**身份、学习对象、目标**（可选 **引擎/环境**）。需要直达某阶段资产或清单时，在句中包含「跳过 / 直达 / 直接提取 / 直接给我 / 直接生成」等措辞，或写明「第 X 阶段」「XX 审查 Checklist」。

执行本 Skill 时，正文须采用**工程技术书面语**：客观、可验证、可绑定引擎机制；避免口语化与宏大叙事。

示例消息：
- 生成游戏开发协同学习规划：身份客户端程序，对象 Unity Addressables 资源管理，目标跑通热更与内存预算。
- 身份 TA，对象 PCG 地图管线，目标在 AI 辅助下搭可维护的生成与验收链，引擎 UE5。
- 直接提取第二阶段的审查清单：由 AI 生成的 C# 状态机代码的内存与 GC 优化。

## 工具
- `tool.py`：生成 Markdown 正文；支持 `--stage` 直达、`--direct-request` 直达描述、`--need-practice` 输出含硬伤的演练材料。

## 参数
- `--input-text`：原始输入，可含「身份/对象/目标/引擎」标签。
- `--identity`：身份（程序/TA/关卡等）。
- `--learning-object`：学习对象或模块。
- `--goal`：最终目标。
- `--engine`：引擎或栈（Unity、UE5、Godot 等）。
- `--stage`：`1` / `2` / `3`，直达对应阶段主体内容。
- `--direct-request`：自然语言直达指令（与 `--stage` 二选一或组合）。若省略但 `--input-text` 中含直达类触发词，脚本会从 `input-text` 推断为直达指令。
- `--need-practice`：输出「对抗性仿真演练」段落（需用户明确要性能/审计演练时使用）。
- `--output-file`：可选，自定义输出路径。

## 输出
- 默认写入：`/tmp/tyclaw_{staff_id}_{timestamp}_game-dev-learning-path-planner/game_dev_learning_path.md`
- stdout 打印正文，并在末尾输出 JSON：`{"output_file": "..."}`
