# AI协同学习路径规划器

## 功能
围绕“人机协同”生成结构化学习路径，明确 AI 执行域与人类主体域，并按阶段输出可直接使用的学习资产。

## 歧义处理（执行约束）

若无法可靠判断用户具体意图（例如：仅泛泛说「想学习」、未区分**通用领域**与**游戏工程**、一句话内多主题并列、或未同时给出可落地的**身份 / 学习对象 / 目标**中至少两项），**禁止猜测参数并调用 `tool.py`**。应先回复**一条追问**，在**同一条消息**中列清需用户补充的 **3 项**（领域分流、身份+对象、可验证目标；符合钉钉「一次列清」交互）；用户补齐后再执行工具。

若判断主题为**游戏工程**（引擎、TA、战斗/资源管线、性能等），应在追问中请用户确认是否改用「**游戏开发人机协同学习路径规划器**」或补充引擎与模块后再继续。

## 使用方式
在钉钉中描述你的身份、学习对象和目标，或直接要求直达某阶段资产。

示例消息：
- 帮我生成学习路径：身份是法务，对象是劳动合同审查，目标是独立输出风险分级报告。
- 直接给我第二阶段的合规审查 Checklist，场景是劳动合同。
- 我想做对抗性演练，主题是投融资尽调报告。

## 工具
- `tool.py`：生成 Markdown 学习路径正文（支持标准流程、动态直达、演练触发）。

## 参数
- `--input-text`：原始输入文本，可包含“身份/对象/目标”标签。
- `--identity`：身份定位。
- `--learning-object`：学习对象。
- `--goal`：最终目标。
- `--stage`：直达阶段（`1`/`2`/`3`）。
- `--direct-request`：直达资产请求（如“第二阶段 checklist”）。
- `--need-practice`：附加输出对抗性仿真演练内容。
- `--output-file`：可选，自定义输出路径。

## 输出
- 默认输出结构化 Markdown 文本，并保存到：
  - `/tmp/tyclaw_{staff_id}_{timestamp}_ai-learning-path-planner/learning_path.md`
- 同时在 stdout 输出正文与 `output_file` 路径（JSON）。
